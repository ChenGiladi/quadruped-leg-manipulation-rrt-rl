# Pinned dependencies for the trained policy in ../models/best_model8800.zip.
# Versions are exactly those embedded in the SB3 checkpoint (system_info.txt).
# Tested target: Python 3.10.11
#
# Install with:   pip install -r requirements.txt
#
# CoppeliaSim is NOT a pip package — install separately from
# https://www.coppeliarobotics.com (4.5.x EDU was used) and point the
# COPPELIASIM_PY_PATH environment variable at its
# programming/zmqRemoteApi/clients/python directory before launching any
# script that imports from zmqRemoteApi.

stable-baselines3==1.8.0
gym==0.21.0
torch==2.0.1
numpy==1.23.5

# Plotting / data
matplotlib>=3.7
scipy>=1.10

# Training-time visualisation (optional at inference time; remove from this
# file or comment out the `import visdom` line in the env scripts if not used)
visdom>=0.2.4
